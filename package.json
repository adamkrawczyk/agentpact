{
  "name": "agentpact",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @agentpact/api && npm run build -w @agentpact/mcp && npm run build -w @agentpact/web && npm run build -w agentpact-daemon",
    "dev": "concurrently \"npm run dev -w @agentpact/api\" \"npm run dev -w @agentpact/mcp\" \"npm run dev -w @agentpact/web\" \"npm run dev -w agentpact-daemon\"",
    "daemon": "tsx scripts/agentpact-daemon.ts",
    "migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "seed:challenges": "tsx scripts/seed-challenges.ts",
    "test": "npm run test -w @agentpact/api && npm run test -w agentpact-daemon",
    "e2e:onchain": "tsx scripts/e2e-onchain-test.ts",
    "sdk:generate": "bash scripts/generate-sdks.sh",
    "sdk:publish": "bash scripts/generate-sdks.sh && cd ../agentpact-sdk && npm run build && npm publish --access public && cd ../agentpact-python-sdk && python -m build && twine upload dist/*"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "@testcontainers/postgresql": "^11.13.0",
    "@types/node": "^22.13.10",
    "chai": "^4.5.0",
    "concurrently": "^9.1.2",
    "dotenv": "^17.2.4",
    "ethers": "^6.16.0",
    "hardhat": "^2.28.4",
    "testcontainers": "^11.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "zod": "^3.24.2"
  }
}
